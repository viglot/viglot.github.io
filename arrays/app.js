let array =["csgo", "R6s", "valorant"]

/*array.push lägger till en ny text, i slutet av arrayn*/
array.push("LoL")

array.push("Z1")

/* array.pop tar bort den sista texten*/
array.pop()

/*array.splice tar bort text från 3 uppåt*/
array.splice(3)

document.write(array)